# TypeScript + webpack = ♥

## Usage

```bash
$ git clone --depth=1 git://github.com/tamamu/ts-webpack-boilerplate NewProject
$ cd NewProject
$ rm -rf .git

$ yarn
$ yarn dev
$ yarn test
$ yarn build
```
